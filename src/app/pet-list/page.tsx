import FilterPageComponent from "@/components/FilterPageComponent";

export default function animalsPage() {
  return (
    <main className="flex flex-col flex-auto h-full flex-shrink-0 items-center bg-bgColor px-[10px] gap-y-5">
      <FilterPageComponent />
    </main>
  );
}
